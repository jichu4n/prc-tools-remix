
/* Define if you have a <sys/wait.h> which is POSIX.1 compatible.  */
#undef HAVE_SYS_WAIT_H 

/* Define various configure options so they can be seen by C code without
   massive -Dfoo=bar lists.  EXEC_PREFIX would be in this list too, but it
   is set too late by configure, so must be done with -DEXEC_PREFIX=bar in
   the Makefile.  */

#undef TARGET
#undef TARGET_ALIAS
#undef PALMDEV_PREFIX
#undef PALMDEV_PATH
#undef PRC_TOOLS_VERSION
#undef EXEEXT
