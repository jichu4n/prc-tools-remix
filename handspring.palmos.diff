(BTW, we're depending on alphabetical order here: this patch has to be
applied *after* gcc-2.95.2.palmos.diff.)

--- gcc-2.95.2/gcc/config/m68k/m68kpalmos.h.orig	Mon Jun 26 17:05:07 2000
+++ gcc-2.95.2/gcc/config/m68k/m68kpalmos.h	Mon Jun 26 17:05:31 2000
@@ -199,7 +199,8 @@
 /* Add Palm OS related but not Palm SDK related header directories here.  */
 
 #define PALMDEV_INCLUDES "\
--isystem %(palmdev_prefix)include"
+-isystem %(palmdev_prefix)include \
+-isystem %(palmdev_prefix)include/Handspring"
 
 
 #define PALMDEV_SDK_INCLUDES "\
